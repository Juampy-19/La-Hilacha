<div class="burgerMenu" id="burgerMenu">
    <i class="fas fa-bars"></i>
    <div id="navMenu">
        <ul>
            <% if (activePage !== 'home') { %>
                <li><a href="/">Home</a></li>
            <% } %>
            <% if (activePage !== 'pizzas') { %>
                <li><a href="/pizzas">Pizzas</a></li>
            <% } %>
            <% if (activePage !== 'empanadas') { %>
                <li><a href="/empanadas">Empanadas</a></li>
            <% } %>
            <% if (activePage !== 'seleccionProductos') { %>
                <li><a href="/seleccionProductos">Pedí</a></li>
            <% } %>
        </ul>
    </div>
</div>

<div class="contButton">
    <% if (activePage !== 'home') { %>
        <a href="/"><button class="button">Home</button></a>
    <% } %>

    <% if (activePage !== 'pizzas') { %>
        <a href="/pizzas"><button class="button">Pizzas</button></a>
    <% } %>

    <% if (activePage !== 'empanadas') { %>
        <a href="/empanadas"><button class="button">Empanadas</button></a>
    <% } %>

    <% if (activePage !== 'seleccionProductos') { %>
        <a href="/seleccionProductos"><button class="button">Pedí</button></a>
    <% } %>
</div>