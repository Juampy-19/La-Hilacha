<!DOCTYPE html>
<html lang="en">

    <%- include('./partials/head.ejs') %>

<body>

    <%- include('./partials/header.ejs') %>

    <main>

        <%- include('./partials/menu.ejs', { activePage: 'seleccionProductos' } ) %>

        <form action="/seleccionProductos" method="POST" class="pedido">
            <section class="selecEmpanadas">
                <div class="titleForm">
                    <h2>Empanadas</h2>
                    <i class="fas fa-arrow-circle-right toggle-menu" data-menu="empanadas"></i>
                </div>

                <div class="menu empanadas-menu">

                    <% if (loading) { %>

                         <%- include('./partials/loader.ejs') %>

                    <% } else { %>

                        <%- include('./partials/selecEmpanadaCategoria.ejs', { tituloCategoria: 'Tradicionales', empanadas: empanadasData.tradicionales, precios: empanadasData.precioTradicionales }) %>
                        <%- include('./partials/selecEmpanadaCategoria.ejs', { tituloCategoria: 'Canastitas', empanadas: empanadasData.canastitas, precios: empanadasData.precioCanastitas }) %>
                        <%- include('./partials/selecEmpanadaCategoria.ejs', { tituloCategoria: 'Premium', empanadas: empanadasData.premium, precios: empanadasData.precioPremium }) %>

                    <% } %>
                    
                </div>
            </section>

            <section class="selecPizzas">
                <div class="titleForm">
                    <h2>Pizzas</h2>
                    <i class="fas fa-arrow-circle-right toggle-menu" data-menu="pizzas"></i>
                </div>

                <div class="menu pizzas-menu">

                    <% if (loading) { %>

                        <%- include('./partials/loader.ejs') %>

                    <% } else { %>

                        <div class="selecPizzaTamaño">
                            <div class="titleForm">
                                <h3>Pizzas Gigantes</h3>
                                <i class="fas fa-arrow-circle-right toggle-menu" data-menu="gigantes"></i>
                            </div>

                            <div class="menu gigantes-menu">
                                <%- include('./partials/selecPizzaCategoria.ejs', { tituloCategoria: 'Eternas', pizzas: pizzasData.eternas, precios: pizzasData.precioEternas, filtroTamaño: 'Gigante', dividirPrecio: false }) %>
                                <%- include('./partials/selecPizzaCategoria.ejs', { tituloCategoria: 'Clásicas', pizzas: pizzasData.clasicas, precios: pizzasData.precioClasicas, filtroTamaño: 'Gigante', dividirPrecio: false }) %>
                                <%- include('./partials/selecPizzaCategoria.ejs', { tituloCategoria: 'Tradicionales', pizzas: pizzasData.tradicionales, precios: pizzasData.precioTradicionales, filtroTamaño: 'Gigante', dividirPrecio: false }) %>
                                <%- include('./partials/selecPizzaCategoria.ejs', { tituloCategoria: 'Especiales', pizzas: pizzasData.especiales, precios: pizzasData.precioEspeciales, filtroTamaño: 'Gigante', dividirPrecio: false }) %>
                            </div>
                            <div class="titleForm">
                                <h3>Pizzas Gigantes - 2 sabores</h3>
                                <i class="fas fa-arrow-circle-right toggle-menu" data-menu="gigantes2"></i>
                            </div>

                            <div class="menu gigantes2-menu">
                                <%- include('./partials/selecPizzaCategoria.ejs', { tituloCategoria: 'Eternas', pizzas: pizzasData.eternas, precios: pizzasData.precioEternas, filtroTamaño: 'Gigante', dividirPrecio: true }) %>
                                <%- include('./partials/selecPizzaCategoria.ejs', { tituloCategoria: 'Clásicas', pizzas: pizzasData.clasicas, precios: pizzasData.precioClasicas, filtroTamaño: 'Gigante', dividirPrecio: true }) %>
                                <%- include('./partials/selecPizzaCategoria.ejs', { tituloCategoria: 'Tradicionales', pizzas: pizzasData.tradicionales, precios: pizzasData.precioTradicionales, filtroTamaño: 'Gigante', dividirPrecio: true }) %>
                                <%- include('./partials/selecPizzaCategoria.ejs', { tituloCategoria: 'Especiales', pizzas: pizzasData.especiales, precios: pizzasData.precioEspeciales, filtroTamaño: 'Gigante', dividirPrecio: true }) %>
                            </div>

                            <div class="titleForm">
                                <h3>Pizzas Gigantes - 4 sabores</h3>
                                <i class="fas fa-arrow-circle-right toggle-menu" data-menu="gigantes4"></i>
                            </div>

                            <div class="menu gigantes4-menu">
                                <%- include('./partials/selecPizzaCategoria.ejs', { tituloCategoria: 'Eternas', pizzas: pizzasData.eternas, precios: pizzasData.precioEternas, filtroTamaño: 'Gigante', dividirPrecio: 4 }) %>
                                <%- include('./partials/selecPizzaCategoria.ejs', { tituloCategoria: 'Clásicas', pizzas: pizzasData.clasicas, precios: pizzasData.precioClasicas, filtroTamaño: 'Gigante', dividirPrecio: 4 }) %>
                                <%- include('./partials/selecPizzaCategoria.ejs', { tituloCategoria: 'Tradicionales', pizzas: pizzasData.tradicionales, precios: pizzasData.precioTradicionales, filtroTamaño: 'Gigante', dividirPrecio: 4 }) %>
                                <%- include('./partials/selecPizzaCategoria.ejs', { tituloCategoria: 'Especiales', pizzas: pizzasData.especiales, precios: pizzasData.precioEspeciales, filtroTamaño: 'Gigante', dividirPrecio: 4 }) %>
                            </div>

                            <div class="titleForm">
                                <h3>Pizzas Grandes</h3>
                                <i class="fas fa-arrow-circle-right toggle-menu" data-menu="grandes"></i>
                            </div>

                            <div class="menu grandes-menu">
                                <%- include('./partials/selecPizzaCategoria.ejs', { tituloCategoria: 'Eternas', pizzas: pizzasData.eternas, precios: pizzasData.precioEternas, filtroTamaño: 'Grande', dividirPrecio: false }) %>
                                <%- include('./partials/selecPizzaCategoria.ejs', { tituloCategoria: 'Clásicas', pizzas: pizzasData.clasicas, precios: pizzasData.precioClasicas, filtroTamaño: 'Grande', dividirPrecio: false }) %>
                                <%- include('./partials/selecPizzaCategoria.ejs', { tituloCategoria: 'Tradicionales', pizzas: pizzasData.tradicionales, precios: pizzasData.precioTradicionales, filtroTamaño: 'Grande', dividirPrecio: false }) %>
                                <%- include('./partials/selecPizzaCategoria.ejs', { tituloCategoria: 'Especiales', pizzas: pizzasData.especiales, precios: pizzasData.precioEspeciales, filtroTamaño: 'Grande', dividirPrecio: false }) %>
                            </div>

                            <div class="titleForm">
                                <h3>Pizzas Grandes - 2  sabores</h3>
                                <i class="fas fa-arrow-circle-right toggle-menu" data-menu="grandes2"></i>
                            </div>

                            <div class="menu grandes2-menu">
                                <%- include('./partials/selecPizzaCategoria.ejs', { tituloCategoria: 'Eternas', pizzas: pizzasData.eternas, precios: pizzasData.precioEternas, filtroTamaño: 'Grande', dividirPrecio: true }) %>
                                <%- include('./partials/selecPizzaCategoria.ejs', { tituloCategoria: 'Clásicas', pizzas: pizzasData.clasicas, precios: pizzasData.precioClasicas, filtroTamaño: 'Grande', dividirPrecio: true }) %>
                                <%- include('./partials/selecPizzaCategoria.ejs', { tituloCategoria: 'Tradicionales', pizzas: pizzasData.tradicionales, precios: pizzasData.precioTradicionales, filtroTamaño: 'Grande', dividirPrecio: true }) %>
                                <%- include('./partials/selecPizzaCategoria.ejs', { tituloCategoria: 'Especiales', pizzas: pizzasData.especiales, precios: pizzasData.precioEspeciales, filtroTamaño: 'Grande', dividirPrecio: true }) %>
                            </div>

                            <div class="titleForm">
                                <h3>Pizzas Chicas</h3>
                                <i class="fas fa-arrow-circle-right toggle-menu" data-menu="chicas"></i>
                            </div>

                            <div class="menu chicas-menu">
                                <%- include('./partials/selecPizzaCategoria.ejs', { tituloCategoria: 'Eternas', pizzas: pizzasData.eternas, precios: pizzasData.precioEternas, filtroTamaño: 'Chica', dividirPrecio: false }) %>
                                <%- include('./partials/selecPizzaCategoria.ejs', { tituloCategoria: 'Clásicas', pizzas: pizzasData.clasicas, precios: pizzasData.precioClasicas, filtroTamaño: 'Chica', dividirPrecio: false }) %>
                                <%- include('./partials/selecPizzaCategoria.ejs', { tituloCategoria: 'Tradicionales', pizzas: pizzasData.tradicionales, precios: pizzasData.precioTradicionales, filtroTamaño: 'Chica', dividirPrecio: false}) %>
                                <%- include('./partials/selecPizzaCategoria.ejs', { tituloCategoria: 'Especiales', pizzas: pizzasData.especiales, precios: pizzasData.precioEspeciales, filtroTamaño: 'Chica', dividirPrecio: false }) %>
                            </div>
                        </div>

                    <% } %>

                </div>
            </section>

            <button type="submit" class="button">Agregar al carrito</button>

        </form>
    </main>

    <%- include('./partials/footer.ejs') %>
    
</body>
    <script src="/js/menu.js"></script>
    <script src="/js/seleccionProductos.js"></script>
</html>